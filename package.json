{
  "name": "royal-bistro-restaurant-management",
  "version": "2.0.0",
  "description": "Hệ thống Quản lý Nhà hàng & Gọi món (Fullstack JavaScript: Node.js Express + React Vite)",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"cyan,amber\" \"npm run dev:backend\" \"npm run dev:frontend\"",
    "start": "cd backend && npm start",
    "build:frontend": "cd frontend && npm run build",
    "build": "npm run build:frontend",
    "install:all": "cd backend && npm install && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
